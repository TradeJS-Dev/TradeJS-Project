interface:
  display_name: "Strategy Release (Deprecated)"
  short_description: "Route to one focused strategy workflow"
  default_prompt: "Use $strategy-release only as a compatibility router. Infer or ask for exactly one focused candidate, improvement, period, forward, or risk workflow; never combine research and production mutation."
